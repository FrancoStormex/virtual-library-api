/**
 * ES:
 * Grupo de paquetes con los validadores de atributos.
 * Estos validadores están compuestos por una class que posee la implementación y por una annotation que permitirá incrustar información suplementaria en el código fuente.
 *
 * EN:
 * Package group with attribute validators.
 * These validators are composed of a class that owns the implementation and an annotation that will allow embedding additional information in the source code.
 */
package ar.com.stormex.app.library.validator;