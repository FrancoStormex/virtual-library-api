/**
 * ES:
 * Grupo de paquetes donde están los dto generales para los servicios.
 * Los DTO son un tipo de objeto que se utilizan únicamente para transportar datos.
 * El DTO contiene las propiedades de un objeto.
 *
 * EN:
 * Group of packages where the general DTOs for the services are.
 * DTOs are a type of object that are used solely to transport data.
 * The DTO contains the properties of an object.
 */
package ar.com.stormex.app.library.dto;