/**
 * ES:
 * Grupo de paquetes con los mapper para los servicios.
 * Para el mapeo entre clases utiizaremos MapStruct.
 * MapStruct lo usaremos para realizar algún tipo de conversión entre objetos de Java, por ejemplo, si tenemos un dto y queremos transformarlo en una entidad.
 *
 * EN:
 * Group of packages with the mappers for the services.
 * For the mapping between classes we will use MapStruct.
 * We will use MapStruct to perform some kind of conversion between Java objects, for example, if we have a dto and we want to transform it into an entity.
 */
package ar.com.stormex.app.library.mapper;