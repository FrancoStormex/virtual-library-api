/**
 * ES:
 * Grupo de paquetes con los controladores de cada servicio.
 * Cada controller es responsable de procesar las llamadas entrantes (request) que ingresan a nuestra aplicacion, validarlas y dar una respuesta (response).
 *
 * EN:
 * Group of packages with the controllers of each service.
 * Each controller is responsible for processing the incoming calls (request) that enter our application, validating them and giving a response (response).
 */
package ar.com.stormex.app.library.controller;